# Submission Attack Log

Updated: 2026-06-13

## Attack Rounds

1. Closest-prior attack: POMDPs, contact-rich manipulation, active perception, and fixture-aware planning already cover much of the conceptual area. Response: keep the novelty to an interpretable fixture variable tied directly to release actions.
2. Evidence attack: the experiment is a toy simulator with synthetic residuals. Response: mark the result workshop-only / strong-revise.
3. Taxonomy attack: the fixture classes and release actions are hand-coded. Response: add a v2 out-of-taxonomy fixture stress.
4. Unknown-fixture attack: an unseen cable tie is not in the known fixture menu and requires an absent action. Response: forced known-taxonomy inference fails in 2000/2000 seeds; guarded inference avoids damage only by abstaining in 1999/2000 seeds and still has one false commit.
5. Artifact attack: v1 retained a tracked local `main.pdf` and stale Desktop-copy status. Response: add a build script that copies to Downloads and removes local `main.pdf`; Desktop copies are absent.

## V2 Outcome

The paper remains workshop-only / strong-revise. The supported claim is narrow: a calibrated latent fixture state can reduce wrong release actions when the hidden cause belongs to the known taxonomy. The v2 stress explicitly rejects any claim of general unknown-fixture discovery.
